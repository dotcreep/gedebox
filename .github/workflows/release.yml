# .github/workflows/release.yaml

name: Build and Release

on:
  release:
    types: [created]
  push:
    branches: [main]

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        arch: [amd64, arm64]

    steps:
    - name: Setup Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.23.2'

    - name: Checkout code
      uses: actions/checkout@v3

    - name: Build
      run: go build -o gedebox-${{ matrix.os }}-${{ matrix.arch }} main.go

    - name: Rename for Windows
      if: matrix.os == 'windows-latest'
      run: rename gedebox-${{ matrix.os }}-${{ matrix.arch }}.exe

    - name: Create Release
      id: create_release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: v1.0.1-alpha
        release_name: Release v1.0.1-alpha
        body: |
          New Release Gedebox with version v1.0.1-alpha.
        draft: false
        prerelease: false

    - name: Upload Release Asset on Windows
      if: matrix.os == 'windows-latest'
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: ./gedebox-${{ matrix.os }}-${{ matrix.arch }}.exe
        asset_name: gedebox-${{ matrix.os }}-${{ matrix.arch }}.exe
        asset_content_type: application/octet-stream

    - name: Upload Release Asset
      if: matrix.os != 'windows-latest'
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: ./gedebox-${{ matrix.os }}-${{ matrix.arch }}
        asset_name: gedebox-${{ matrix.os }}-${{ matrix.arch }}
        asset_content_type: application/octet-stream